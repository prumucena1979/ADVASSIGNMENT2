# --- Review Length Distribution ---

train_df["review_length"] = train_df["review"].str.len()

print("\n--- Review Length Summary (characters) ---")
display(train_df["review_length"].describe())

plt.figure(figsize=(8, 4))
sns.histplot(train_df["review_length"], bins=50, kde=True)
plt.title("Distribution of Review Lengths (All Reviews)")
plt.xlabel("Characters")
plt.ylabel("Frequency")
plt.xlim(0, 1000)
plt.show()

# By sentiment
plt.figure(figsize=(8, 4))
sns.kdeplot(
    data=train_df, x="review_length",
    hue="sentiment", common_norm=False,
    fill=True, alpha=0.4
)
plt.title("Review Length by Sentiment")
plt.xlabel("Characters")
plt.xlim(0, 1000)
plt.show()

print("Business implication:")
print("- Most reviews are relatively short (< 500 chars), which supports using models with")
print("  moderate max sequence lengths (e.g., 128–256 tokens).")
print("- Similar length distributions across sentiments suggest that sentiment is driven more")
print("  by word choice than by length.")
